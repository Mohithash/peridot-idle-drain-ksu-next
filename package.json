{
  "name": "peridot-idle-drain-ksu-next",
  "private": true,
  "version": "1.7.7",
  "description": "KernelSU Next WebUI build for Peridot Idle Drain Tweaks",
  "scripts": {
    "build:webui": "esbuild webroot-src/app.js --bundle --format=iife --target=es2017 --outfile=webroot/app.js --log-level=warning",
    "build:zip": "node tools/build-zip.mjs",
    "build": "npm run build:zip"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "kernelsu": "^3.0.2",
    "yazl": "^3.3.1"
  }
}
